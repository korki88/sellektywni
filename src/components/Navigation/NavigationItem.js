import React, {useRef} from 'react'
import gsap from 'gsap'

export default function NavigationItem( section ) {
    const htmlContent = null;
    const linkToContent = null

    return (
        <div>D
            {section.name}
        </div>
    )
}
